App({
  onLaunch() {},
  globalData: {
    // WeChat DevTools on Windows: use 127.0.0.1 instead of localhost
    // Production: change to your HTTPS domain, e.g. 'https://cookbook.example.com/'
    h5BaseUrl: 'http://127.0.0.1:3000/'
  }
});
